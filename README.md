# CRUD
C# CRUD MVC application for reading and fetching data from SQL (dabber for DB connection)

The purpose of this application is to perform CRUD operations in an MVC application using the ASP.NET core 2.2 framework in C#.
